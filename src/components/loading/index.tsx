import { CircularProgress } from '@mui/material'
import { StyledLoading } from './styles'

const Loading = () => {
  return (
    <StyledLoading>
        <CircularProgress />
    </StyledLoading>
  )
}

export default Loading
